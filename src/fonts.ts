import { Inter, Open_Sans } from 'next/font/google';;

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
  })

export const openSans = Open_Sans({
    subsets: ['latin'],
    display: 'swap',
})